package com.rootnetsec.cryptofile;

/**
 * The class that runs the main method form the App class.
 * This class exist only because of bug that prevents to run a class that
 inherits from JavaFX Application in jar files.
 */
public class Main {

    public static void main(String[] args) {
        App.main(args);
    }
}